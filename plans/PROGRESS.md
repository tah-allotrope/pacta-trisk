# PACTA + TRISK Vietnam — Status

> **As of:** 2026-08-27

Waves 0, 1 and 2 are complete. The platform is at version 0.4.1: two
engagements (`mcb-demo`, `sdb-rehearsal`) run end to end through one
orchestrator (`scripts/run_engagement.R`), gated in CI by a byte-identity
check and six cross-artifact invariants (`tools/verify_refactor.R`).

`NEWS.md` is the authoritative, actively-maintained changelog — read it for
what shipped in each wave and phase. This file is intentionally a pointer,
not a duplicate.

The current program of work is
`plans/2026-08-26-wave3-convergence-vintage-delivery-plan.md` ("Wave 3:
Convergence, Scenario Vintage Truth, and Delivery Readiness"), which merges
three prior brainstorm documents into one sequenced, phased plan with a
single golden-refreeze boundary. Check that file's phase checkboxes for the
current state of in-progress work rather than this one.
