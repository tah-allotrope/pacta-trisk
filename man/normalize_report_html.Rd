% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report_fingerprint.R
\name{normalize_report_html}
\alias{normalize_report_html}
\title{Replace every generated timestamp, date and git SHA in an HTML document
with a fixed placeholder, so two runs of the same pipeline compare equal.}
\usage{
normalize_report_html(html)
}
\arguments{
\item{html}{character — one or more HTML documents (or fragments).}
}
\value{
character — same length as \code{html}, with volatile spans replaced by
\verb{<TIMESTAMP>}, \verb{<DATE>} or \verb{<SHA>} and CRLF normalized to LF.
}
\description{
Substitution order matters. ISO-8601 (with a \code{T} separator) is replaced
first, because a looser \verb{YYYY-MM-DD HH:MM} pattern would otherwise consume
its date half. \code{HH:MM:SS} is replaced before \code{HH:MM}, for the same reason.
}
