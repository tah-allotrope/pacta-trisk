% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trisk_core.R
\name{resolve_trisk_paths}
\alias{resolve_trisk_paths}
\title{Resolve a sector's TRISK input/output directories.}
\usage{
resolve_trisk_paths(
  sector,
  output_root = NULL,
  input_root = "output/trisk_inputs",
  trisk_output_root = "synthesis_output/trisk"
)
}
\arguments{
\item{sector}{character.}

\item{output_root}{character|NULL — explicit output dir, or NULL to
derive from \code{trisk_output_root}.}

\item{input_root}{character — root of the prepared TRISK inputs, default
"output/trisk_inputs" (today's literal).}

\item{trisk_output_root}{character — root of TRISK run outputs, default
"synthesis_output/trisk" (today's literal); only used when output_root
is NULL.}
}
\value{
list(input_dir, output_root).
}
\description{
Resolve a sector's TRISK input/output directories.
}
