% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/step_registry.R
\name{resolve_step_list}
\alias{resolve_step_list}
\title{Resolve the ordered step list for an engagement run.}
\usage{
resolve_step_list(cfg, ctx)
}
\arguments{
\item{cfg}{list — the loaded engagement config.}

\item{ctx}{list — effective_config_path, run_intake, raw_loanbook,
intake_dir, top_n.}
}
\value{
list of list(name, script, args).
}
\description{
When \code{cfg$steps} is non-empty, it names the steps to run, in order,
overriding the boolean-flag translation entirely (per-sector
\verb{trisk_sector_demo_<sector>} names are still expanded against
\code{cfg$trisk_sectors}). When empty (the default for every config written
before this phase), falls back to the exact flag-driven behavior of the
original build_step_list(), so every existing config keeps working
unchanged.
}
