% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engagement_plan.R
\name{enforce_manifest_policy}
\alias{enforce_manifest_policy}
\title{Enforce the manifest policy decided by plan_engagement_run().}
\usage{
enforce_manifest_policy(plan, allow_partial_manifest)
}
\arguments{
\item{plan}{list — plan_engagement_run() output.}

\item{allow_partial_manifest}{logical(1) — cli$allow_partial_manifest.}
}
\value{
invisible TRUE; calls stop() only when a filtered run would
clobber a complete public manifest without the opt-in flag.
}
\description{
A filtered run's manifest describes only the steps it ran. Writing that
over a complete PUBLIC manifest silently destroys the provenance record,
so the orchestrator refuses without an explicit opt-in (verbatim from
scripts/run_engagement.R). Filesystem reads and the refusal stop() live
here, at the edge; the decision inputs live in the plan.
}
